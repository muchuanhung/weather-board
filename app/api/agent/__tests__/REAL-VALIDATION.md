# 真實服務驗證：2026-09-24

時間：台灣時間 01:38–01:39。直接呼叫本地 `POST` handler，使用本機設定的真實 CWA 與 Anthropic 服務；非 production 部署、非瀏覽器驗收。沒有輸出金鑰。

共 3 次 Agent 請求、6 次模型請求（每题兩輪），Anthropic 與 CWA HTTP 全部 200。模型為 `claude-sonnet-5`。合計 input 6,943 tokens、output 1,030 tokens（含 thinking）；未換算費用。

| 提問                                     | 實際工具選擇                              | 結果                                                                                                   |
| ---------------------------------------- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| 今天臺北會不會很熱？要怎麼穿？           | 臺北市，offset 0                          | 6.85 秒，mode=llm。回答 31/24°C、降雨 10%，與證據一致，有穿衣建議。回答比原本 1–2 句長，包含條列。     |
| 週末去台北還是宜蘭比較適合？             | 臺北市與宜蘭縣，offset 2、3（9/26、9/27） | 7.13 秒，原版本 mode=llm；城市與日期正確，但週日雨量缺值，模型仍推薦整個週末去臺北。此項原版本不通過。 |
| 我從台北出發去宜蘭玩，那邊今天天氣如何？ | 只查宜蘭縣，offset 0                      | 4.20 秒，mode=llm。沒有因沒查出發地而丟掉答案；30/23°C、降雨 20%，與證據一致。                         |

## 實測發現後的修正

新增後端 `hasComparableEvidence`，比較問題須具備所有城市、相同日期，以及各日期完整的降雨機率和高低溫；否則走規則備援。不能只依賴 prompt 讓模型自行判斷是否可以推薦。

- 把上述真實週末證據帶入新檢查，結果為 false，會拒絕採用過度推薦的 LLM 回答。
- 新增自動測試：城市缺少、日期不同、缺雨量、缺溫度、無日資料均不能通過。
- POST 模擬模型即使推薦臺北，只要 CWA 雨量缺值，最終仍回 `mode: rules` 且說明「無法可靠推薦」。

修正後只做真實證據重播與 mock POST 回歸，沒有再追加付費 LLM 呼叫。此紀錄不代表所有自然語言問題皆正確，也不代表線上限流已設定。

## 驗證界線

沙箱第一次無法 DNS 解析（ENOTFOUND），當次未連到服務，HTTP handler 回 502；放行網路後才執行上述三題。未在瀏覽器檢查長回答的聊天泡泡版面。公開 API 的 WAF／費用限制仍需統籌確認。
